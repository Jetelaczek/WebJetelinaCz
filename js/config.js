// Third-party service settings. Both values are public by design and safe to commit.
const SITE_CONFIG = {
  // Web3Forms access key registered to the Contact inbox (https://web3forms.com).
  // Empty: the form tells Visitors to email directly instead.
  web3formsAccessKey: "",
  // GoatCounter site code, the "<code>" in https://<code>.goatcounter.com. Empty: no analytics.
  goatcounterCode: "",
};
